let projectId = document.getElementById('projectId');

projectId.style.backgroundColor = 'cyan';

projectId.style.margin = '40px';

projectId.style.padding = '20px';

projectId.style.fontSize = '20px';

projectId.style.fontWeight = 'bold';

projectId.style.height = '200px';

projectId.style.width = '300px';

console.log(projectId);